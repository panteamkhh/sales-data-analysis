[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sales-data-analysis"
version = "1.0.0"
description = "Exploratory Python analysis and a Power BI dashboard for a retail sales dataset."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Panteamkhh", email = "pantea.mkh18@gmail.com" }]
keywords = ["data-analysis", "pandas", "sales", "power-bi", "portfolio"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Information Analysis",
]
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "matplotlib>=3.7",
    "seaborn>=0.12",
    "openpyxl>=3.1",
    "Pillow>=10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.5",
    "black>=24.0",
    "jupyter>=1.0",
    "nbstripout>=0.7",
    "pre-commit>=3.7",
]

[project.urls]
Homepage = "https://github.com/panteamkhh/sales-data-analysis"
Repository = "https://github.com/panteamkhh/sales-data-analysis"
Issues = "https://github.com/panteamkhh/sales-data-analysis/issues"

[project.scripts]
sales-analysis = "src.run_analysis:main"

[tool.setuptools]
packages = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = ["ignore::DeprecationWarning"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "W"]

[tool.black]
line-length = 100
target-version = ["py310"]
